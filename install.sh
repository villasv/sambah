#!/bin/sh

# This is the only file that should be in english. It is read by the system that
# is not ready to comprehend portuguese yet.

SAMBAH_DIR="$HOME/.sambá" # Urgh... SAMBÁ_DIR is not a valid identifier!
git clone https://github.com/villasv/sambah.git "$SAMBAH_DIR"
